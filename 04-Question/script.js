//? Declare a variable and assign value to it and check if the string contains a word script using include() method

let str =
  "The includes method returns true if a string contains a specified string";
console.log(str.includes("method"));
